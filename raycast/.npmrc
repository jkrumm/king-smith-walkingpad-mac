# Refuse to install when Node is out of range. @raycast/api 1.104+ requires
# Node >= 22.22.2 — silently warning was a footgun, this turns it into a
# hard error so we catch the mismatch before shipping.
engine-strict=true
fund=false
